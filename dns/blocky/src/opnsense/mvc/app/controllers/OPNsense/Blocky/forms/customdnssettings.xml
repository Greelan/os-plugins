<form>
    <field>
        <id>blocky.general.customTTL</id>
        <label>Custom TTL</label>
        <type>text</type>
        <hint>1h</hint>
        <help>
            <![CDATA[TTL returned for answers coming from the entries on the <a href="/ui/blocky/settings/customdns#overridestab">Host Overrides</a> tab.]]>
        </help>
    </field>
    <field>
        <id>blocky.general.filterUnmappedTypes</id>
        <label>Filter unmapped types</label>
        <type>checkbox</type>
        <help>For a mapped name, return an empty answer for record types you have not provided (e.g. no AAAA when only an IPv4 address is mapped) instead of forwarding the query upstream.</help>
    </field>
    <field>
        <id>blocky.general.customZone</id>
        <label>Zone file</label>
        <type>textbox</type>
        <height>8</height>
        <advanced>true</advanced>
        <help>
            <![CDATA[Optional DNS zone-file content for advanced records (e.g. CNAME), in standard zone format. Every record needs a TTL, either its own or a $TTL line before it, and Blocky refuses to start if the zone does not parse. Applied in addition to the simple entries on the <a href="/ui/blocky/settings/customdns#overridestab">Host Overrides</a> tab.]]>
        </help>
    </field>
</form>
